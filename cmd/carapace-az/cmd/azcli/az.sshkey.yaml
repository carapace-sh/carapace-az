# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: sshkey
description: Manage ssh public key with vm
commands:
    - name: create
      description: Create a new SSH public key resource.
      flags:
        --encryption-type=: The encryption type of the SSH keys to be generated.
        --public-key=: SSH public key used to authenticate to a virtual machine through ssh.
        --subscription=: Name or ID of subscription.
        --tags=:
            description: 'space-separated tags: key[=value] [key[=value] ...].'
            nargs: -1
        -g, --resource-group=!: Name of resource group.
        -l, --location=: Location.
        -n, --name=!: The name of the SSH public key.
      completion:
        flag:
            encryption-type:
                - RSA
                - Ed25519
    - name: delete
      description: Delete an SSH public key.
      flags:
        --ids=:
            description: One or more resource IDs (space-delimited).
            nargs: -1
        --subscription=: Name or ID of subscription.
        -g, --resource-group=: Name of resource group.
        -n, --name=: The name of the SSH public key.
        -y, --yes=: Do not prompt for confirmation.
    - name: list
      description: List all of the SSH public keys.
      flags:
        --subscription=: Name or ID of subscription.
        -g, --resource-group=: Name of resource group.
    - name: show
      description: Retrieve information about an SSH public key.
      flags:
        --ids=:
            description: One or more resource IDs (space-delimited).
            nargs: -1
        --subscription=: Name or ID of subscription.
        -g, --resource-group=: Name of resource group.
        -n, --name=: The name of the SSH public key.
    - name: update
      description: Update an SSH public key resource.
      flags:
        --ids=:
            description: One or more resource IDs (space-delimited).
            nargs: -1
        --public-key=: SSH public key used to authenticate to a virtual machine through ssh.
        --subscription=: Name or ID of subscription.
        --tags=:
            description: 'space-separated tags: key[=value] [key[=value] ...].'
            nargs: -1
        -g, --resource-group=: Name of resource group.
        -n, --name=: The name of the SSH public key.
