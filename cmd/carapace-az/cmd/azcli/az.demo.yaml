# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: demo
description: Demos for designing, developing and demonstrating Azure CLI.
commands:
    - name: byo-access-token
      description: List resource groups by bringing your own access token
      flags:
        --access-token=!: Your own access token
        --cmd=:
            description: ==SUPPRESS==
            nargs: -1
        --subscription-id=!: Subscription ID under which to list resource groups
        --subscription=: Name or ID of subscription.
    - name: secret-store
      commands:
        - name: load
          description: Load custom data from secret store.
          flags:
            --cmd=:
                description: ==SUPPRESS==
                nargs: -1
            --subscription=: Name or ID of subscription.
        - name: save
          description: Save custom data to secret store.
          flags:
            --cmd=:
                description: ==SUPPRESS==
                nargs: -1
            --subscription=: Name or ID of subscription.
    - name: style
      description: A demo showing supported text styles.
      flags:
        --cmd=:
            description: ==SUPPRESS==
            nargs: -1
        --subscription=: Name or ID of subscription.
        --theme=: The theme to format styled text.
      completion:
        flag:
            theme:
                - dark
                - light
                - cloud-shell
                - none
