# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Create an ExpressRoute gateway.
documentation:
    flag:
        location: 'Location. Values from: `az account list-locations`. You can configure the default location using `az configure --defaults location=<location>`.'
        min-val: 'Minimum number of scale units deployed for gateway.  Default: 2.'
        resource-group: Name of resource group. You can configure the default group using `az configure --defaults group=<name>`
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        tags: 'Space-separated tags: key[=value] [key[=value] ...]. Use "" to clear existing tags.  Support shorthand-syntax, json-file and yaml-file. Try "??" to show more.'
flags:
    --allow-non-vwan-traffic=:
        description: Configures this gateway to accept traffic from non Virtual WAN networks.
        nargs: -1
    --max-val=: Maximum number of scale units deployed for gateway.
    --min-val=: Minimum number of scale units deployed for gateway.
    --no-wait=:
        description: Do not wait for the long-running operation to finish.
        nargs: -1
    --subscription=: Name or ID of subscription.
    --tags=:
        description: 'Space-separated tags: key[=value] [key[=value] ...].'
        nargs: -1
    --virtual-hub=: Name or ID of the virtual hub to associate with the gateway.
    -g, --resource-group=!: Name of resource group.
    -l, --location=: Location.
    -n, --name=!: ExpressRoute gateway name.
completion:
    flag:
        allow-non-vwan-traffic:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        no-wait:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
