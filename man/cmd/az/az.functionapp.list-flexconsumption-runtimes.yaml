# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-flexconsumption-runtimes
description: List available built-in stacks which can be used for function apps on the Flex Consumption plan.
documentation:
    flag:
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
flags:
    --runtime=!: limit the output to just the specified runtime
    --subscription=: Name or ID of subscription.
    -l, --location=!: limit the output to just the runtimes available in the specified location
