# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-available
description: List publicly available extensions.
flags:
    -d, --show-details=: Show the raw data from the extension index.
