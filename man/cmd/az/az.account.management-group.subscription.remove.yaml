# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: remove
description: Remove an existing subscription from a management group.
flags:
    -n, --name=!: ""
    -s, --subscription=!: Name or ID of subscription.
