# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Create a rule.
documentation:
    flag:
        address-pool: Name or ID of the backend address pool. If only one exists, omit to use as default.
        http-listener: Name or ID of the HTTP listener. If only one exists, omit to use as default.
        http-settings: Name or ID of the HTTP settings. If only one exists, omit to use as default.
        priority: Priority of the request routing rule. Supported SKU tiers are Standard_v2, WAF_v2.
        resource-group: Name of resource group. You can configure the default group using `az configure --defaults group=<name>`
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
flags:
    --address-pool=: Name or ID of the backend address pool.
    --gateway-name=!: Name of the application gateway.
    --http-listener=: Name or ID of the HTTP listener.
    --http-settings=: Name or ID of the HTTP settings.
    --no-wait=:
        description: Do not wait for the long-running operation to finish.
        nargs: -1
    --priority=: Priority of the request routing rule.
    --redirect-config=: Name or ID of the redirect configuration to use with the created rule.
    --rewrite-rule-set=: Name or ID of the rewrite rule set.
    --rule-type=: Type of the request routing rule.
    --subscription=: Name or ID of subscription.
    --url-path-map=: Name or ID of the url path map.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: Name of the request routing rule.
completion:
    flag:
        no-wait:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        rule-type:
            - Basic
            - PathBasedRouting
