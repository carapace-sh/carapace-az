# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: Update a new origin group within the specified profile.
documentation:
    flag:
        add: 'Add an object to a list of objects by specifying a path and key value pairs.  Example: `--add property.listProperty <key=value, string or JSON string>`'
        ids: One or more resource IDs (space-delimited). It should be a complete resource ID containing all information of 'Resource Id' arguments. You should provide either --ids or other 'Resource Id' arguments.
        remove: 'Remove a property or an element from a list.  Example: `--remove property.list <indexToRemove>` OR `--remove propertyToRemove`'
        resource-group: Name of resource group. You can configure the default group using `az configure --defaults group=<name>`
        session-affinity-state: Whether to allow session affinity on this host. Valid options are 'Enabled' or 'Disabled'
        set: 'Update an object by specifying a property path and value to set.  Example: `--set property1.property2=<value>`'
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        traffic-restoration-time-to-healed-or-new-endpoints-in-minutes: Time in minutes to shift the traffic to the endpoint gradually when an unhealthy endpoint comes healthy or a new endpoint is added. Default is 10 mins. This property is currently not supported.
flags:
    --add=:
        description: Add an object to a list of objects by specifying a path and key value pairs.
        nargs: -1
    --additional-latency-in-milliseconds=: The additional latency in milliseconds for probes to fall into the lowest latency bucket
    --enable-health-probe=:
        description: Indicates whether to enable probe on the origin group.
        nargs: -1
    --force-string=:
        description: When using 'set' or 'add', preserve string literals instead of attempting to convert to JSON.
        nargs: -1
    --ids=:
        description: One or more resource IDs (space-delimited).
        nargs: -1
    --no-wait=:
        description: Do not wait for the long-running operation to finish.
        nargs: -1
    --probe-interval-in-seconds=: The number of seconds between health probes.Default is 240sec.
    --probe-path=: The path relative to the origin that is used to determine the health of the origin.
    --probe-protocol=: Protocol to use for health probe.
    --probe-request-type=: The type of health probe request that is made.
    --profile-name=: Name of the Azure Front Door Standard or Azure Front Door Premium profile which is unique within the resource group.
    --remove=:
        description: Remove a property or an element from a list.
        nargs: -1
    --sample-size=: The number of samples to consider for load balancing decisions
    --session-affinity-state=: Whether to allow session affinity on this host.
    --set=:
        description: Update an object by specifying a property path and value to set.
        nargs: -1
    --subscription=: Name or ID of subscription.
    --successful-samples-required=: The number of samples within the sample period that must succeed
    --traffic-restoration-time-to-healed-or-new-endpoints-in-minutes=: Time in minutes to shift the traffic to the endpoint gradually when an unhealthy endpoint comes healthy or a new endpoint is added.
    -g, --resource-group=: Name of resource group.
    -n, --name=: Name of the origin group which is unique within the endpoint.
completion:
    flag:
        enable-health-probe:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        force-string:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        no-wait:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        probe-protocol:
            - Http
            - Https
            - NotSet
        probe-request-type:
            - GET
            - HEAD
            - NotSet
        session-affinity-state:
            - Disabled
            - Enabled
